# No runtime secrets are required by the current gift3d.my.id MVP.
# Keep real credentials out of the repository. Add environment variables here
# only when a future server-side feature actually requires them.
